# pi-web 提 PR 原则（踩坑总结）

> 基于向 `agegr/pi-web` 提 PR 的多次被拒和一次合并的经验总结。

---

## 核心原则：小、准、无争议

### ✅ 什么样的 PR 容易合并？

| 特征 | 说明 | 例子 |
|------|------|------|
| **改 1 个文件** | 改动范围最小化 | #66 compressTree: 只改 `sessions/[id]/route.ts` |
| **纯后端修复** | 不加 UI，不加面板，不引入新组件 | #83 扩展修复: 只改 `lib/rpc-manager.ts`，16 行 |
| **没有 bug** | 作者不会花时间测功能，测出 bug 直接关 | #39 被关就是因为 Disable all 出 bug |
| **单一职责** | 一个 PR 只做一件事 | #74 被关因为混入了 3 个无关 commit |
| **无副作用** | 不破坏现有功能 | #38 删 tree 字段 → 破坏了 BranchNavigator |

### ❌ 什么样的 PR 容易被拒？

| 特征 | 原因 | 案例 |
|------|------|------|
| **新功能 + 新 UI** | 审查成本高，易出 bug | ToolsConfig 面板（7 文件、500+ 行） |
| **声称"共享配置"但实际没有** | 功能不完整，作者一眼看穿 | #78: `activeTools` 写了但 CLI 不读 |
| **混入无关改动** | 作者无法直接合并，要求拆 PR | #67、#74 被拒主因 |
| **作者已经在做/已经在修的** | 先查作者最近的 commit | #75 HTML 表格：作者自己已修了 |
| **让用户自己升级依赖** | 作者想控制依赖版本 | #40 运行时版本号：被拒 |
| **引入新依赖** | 增加维护成本 | 作者合 #75 自己时补了 `rehype-sanitize` |
| **多个 commit** | 理想情况：1 个 commit | #78 有 5 个 commit |

---

## 提 PR 前的检查清单

### 1. 先看作者动向
- [ ] 作者最近在改什么？有没有已经在做类似功能？
- [ ] 有没有 issue 讨论过这个需求？
- [ ] 作者对类似 PR 的态度（看之前被 merge/closed 的 PR）

### 2. 控制范围
- [ ] 能不能只改 1 个文件？→ 就改 1 个
- [ ] 能不能只做后端不做 UI？→ 就做后端
- [ ] 能不能不加新依赖？→ 绝不加
- [ ] 删减是否多余的新文件、新组件、新路由

### 3. 保证正确性
- [ ] `tsc --noEmit` 通过
- [ ] 心理过一遍所有边界情况（空数组、null、undefined）
- [ ] 别改 PR 描述里没提到的文件
- [ ] 提交前自己人肉测试一遍所有路径

### 4. 提交规范
- [ ] **1 个 commit**（多用 `git commit --amend` 或 squash）
- [ ] 分支名：`fix/xxx` 或 `feat/xxx`
- [ ] 分支**基于 upstream/main**
- [ ] PR body 写清楚：问题、根因、改动、测试方法
- [ ] **不混入无关改动**（哪怕只是改了个空格）

### 5. 预期管理
- [ ] 第一次没合不代表被否定了 → 可能只是时机不对
- [ ] 作者自己修了类似问题 → 尊重选择，不用再提
- [ ] PR 关了可以重新开（reopen），但更多时候作者希望看到新 PR

---

## 教训速记

```
#66  compressTree v3 → ✅ 合并（1 文件，无 UI，无 bug）
#83  扩展修复       → ✅ 合并（1 文件，16 行，纯后端）
对比：
#39  ToolsConfig    → ❌ 关闭（7 文件，500+ 行，有 bug）
#78  ToolsConfig 重提→ ❌ 关闭（5 commit，功能不完整）
#74  typewriter     → ❌ 关闭（混了 3 个无关 commit）
```

**作者偏好：修 bug ＞ 加功能；后端 ＞ 前端；16 行 ＞ 500 行。**
